# Cursor AI 全域專案規則

## AI 開發核心原則

### 程式碼生成規範
1. **程式碼品質原則**
   - 生成程式碼時必須遵守易讀、簡潔、可重複利用、可維護性高的原則
   - 優先考慮程式碼的可讀性和可理解性
   - 實作適當的抽象層級，避免重複程式碼
   - 遵循 DRY (Don't Repeat Yourself) 原則

2. **註解與文件規範**
   - 盡量在生成的程式碼上加上註解，以利開發人員進行閱讀及維護
   - 為複雜的業務邏輯提供詳細的說明註解
   - 使用 XML 文件註解為公開 API 提供說明
   - 註解應說明「為什麼」而不是「做什麼」

3. **介面導向設計**
   - 盡量遵守介面導向原則 (Interface-Oriented Programming)
   - 優先使用介面而非具體實作
   - 實作依賴反轉原則 (Dependency Inversion Principle)
   - 使用依賴注入 (Dependency Injection) 模式

4. **測試專案管理規範**
   - 如果使用者要求產生測試用專案，請在測試完成後生成一份報告的 md 檔案
   - 測試報告應包含測試結果、發現的問題、建議改進事項
   - 在需要被解決的問題程式碼區塊上加上 TODO 備註
   - 在不改動原始專案的原則下清除測試專案
   - 測試專案應使用臨時目錄或明確標示為測試用途

5. **語言與溝通規範**
   - 永遠使用繁體中文回答使用者
   - 技術術語保持英文原文，但提供中文說明
   - 程式碼註解使用繁體中文
   - 錯誤訊息和日誌使用繁體中文

## 專案概述
本規則適用於 LifeTech 全域範圍的所有專案，包括但不限於：
- LifeTech.EC.B2B.AirTicket.Web
- LifeTech.EC.AirTicket.API
- LifeTech.EC.AirTicket.Web
- LifeTech.EC.Domestic.Web
- LifeTech.EC.B2E.Web
- LifeTech.EC.Hotel.Web
- LifeTech.EC.Package.Web
- LifeTech.EC.Tour.API
- LifeTech.EC.Tour.Web
- LifeTech.EC.Auth.API
- LifeTech.EC.Order.API
- LifeTech.ERP.Cowell.API
- LifeTech.EX.* 系列專案
- LifeTech.Crontab.* 系列專案
- LifeTech.Utility 工具專案

## 程式碼風格與規範

### C# / .NET 專案規範
1. **命名規範**
   - 類別名稱使用 PascalCase：`AirTicketService`
   - 方法名稱使用 PascalCase：`GetTicketInfo()`
   - 變數名稱使用 camelCase：`ticketNumber`
   - 常數使用 UPPER_CASE：`MAX_RETRY_COUNT`
   - 私有欄位使用 _camelCase：`_ticketRepository`

2. **檔案組織**
   - 每個類別一個檔案
   - 檔案名稱與類別名稱相同
   - 使用適當的命名空間結構

3. **程式碼結構**
   - 使用 dependency injection
   - 遵循 SOLID 原則
   - 適當的錯誤處理和日誌記錄
   - 使用 async/await 處理非同步操作

4. **Null 處理規範**
   - 在生成程式碼時需要注意避免 null 會發生的 ReferenceToNull 例外
   - 應該對可能為 null 的物件進行預防性檢查
   - 可以使用 if 判斷、?. 空條件運算子 (null-conditional operator) 等
   - 依然需要保持簡潔及易讀原則
   - 優先使用空條件運算子 (?.) 和空合併運算子 (??) 來簡化 null 檢查
   - 對於集合類型，使用 null 檢查後再進行操作
   - 在方法參數驗證中使用 null 檢查，避免傳入 null 值

### Web 專案規範 (ASP.NET MVC/Web API)
1. **控制器規範**
   - 控制器名稱以 Controller 結尾
   - 使用適當的 HTTP 動詞屬性
   - 實作適當的錯誤處理

2. **視圖規範**
   - 使用 Razor 語法
   - 保持視圖簡潔，複雜邏輯移至控制器或服務層
   - 使用適當的 HTML 語義標籤

3. **模型規範**
   - 使用 Data Annotations 進行驗證
   - 實作適當的 ViewModel 模式
   - 使用強型別模型

### API 專案規範
1. **RESTful API 設計**
   - 使用適當的 HTTP 狀態碼
   - 實作統一的回應格式
   - 使用適當的 HTTP 動詞

2. **安全性**
   - 實作適當的認證和授權
   - 使用 HTTPS
   - 實作適當的 CORS 政策

## 資料庫規範

### ORM 與資料存取規範
1. **ORM 選擇**
   - 連線資料庫需要使用 Dapper 作為 ORM
   - 避免使用 Entity Framework，除非專案已有現有的 EF 實作
   - 使用 Dapper 的 Query 和 Execute 方法進行資料操作
   - 實作適當的 Repository 模式封裝 Dapper 操作

1-1. **SQL Client 套件規範**
   - **必須使用：** `Microsoft.Data.SqlClient` (5.1.x 或更高版本)
   - **禁止使用：** `System.Data.SqlClient` (已棄用且有安全性弱點)
   - **命名空間：** 所有新程式碼必須使用 `using Microsoft.Data.SqlClient;`
   - **遷移指南：**
     * 更新 .csproj 檔案中的套件參考
     * 將所有 `using System.Data.SqlClient;` 改為 `using Microsoft.Data.SqlClient;`
     * 在連線字串中添加 `TrustServerCertificate=True`（開發環境）
     * API 使用方式保持不變（SqlConnection、SqlCommand、SqlDataReader 等）
   - **連線字串設定：**
     * 開發環境：必須添加 `TrustServerCertificate=True`
     * 正式環境：使用正式 SSL 憑證，保持 `TrustServerCertificate=False`
     * 範例：`Data Source=SERVER;Initial Catalog=DB;User ID=user;Password=pwd;TrustServerCertificate=True`
   - **安全性考量：**
     * `System.Data.SqlClient` 有已知的中度和高嚴重性弱點
     * `Microsoft.Data.SqlClient` 是 Microsoft 官方推薦的現代化 SQL Server 資料提供者
     * 提供更好的效能和持續的安全性更新
     * 預設啟用更嚴格的 SSL/TLS 加密驗證

2. **SQL 語法規範**
   - SQL 語法需要使用 SQL Server 2016 版本能夠執行的語法
   - 避免使用 SQL Server 2016 不支援的新功能
   - 確保 SQL 語法向後相容性
   - 使用標準的 T-SQL 語法，避免版本特定的擴展功能

3. **SQL 效能與可讀性**
   - SQL 語法生成時需要考慮到最佳化效能以及易讀性
   - 在需要的地方加上註解說明複雜的查詢邏輯
   - 使用適當的索引提示 (Index Hints) 優化查詢效能
   - 避免 SELECT * 語法，明確指定需要的欄位
   - 使用適當的 JOIN 語法而非子查詢（當效能允許時）
   - 為複雜的 WHERE 條件加上註解說明

4. **SQL 執行限制**
   - Cursor AI Agent 並沒有執行 T-SQL 語法的權限
   - 所有 SQL 語法僅供參考，需要開發人員手動執行
   - 提供 SQL 語法時應包含完整的執行說明
   - 建議在測試環境中先驗證 SQL 語法的正確性

5. **Transaction 交易管理規範**
   - 在使用 .NET 提供的 Transaction 時，需要注意避免分散式交易
   - 需要保持裡面的每個資料庫連線使用相同的連線字串
   - 使用 TransactionScope 時，確保所有資料庫操作使用相同的連線
   - 避免在 TransactionScope 內使用不同資料庫或不同連線字串
   - 優先使用 SqlTransaction 而非 TransactionScope（當只涉及單一資料庫時）
   - 在需要跨多個資料庫操作時，考慮使用 Saga 模式或其他分散式交易替代方案
   - 適當處理交易失敗的 rollback 機制
   - 為了保持易讀性，盡量使用 .NET 提供的 Transaction 功能
   - 在連線資料庫時需要使用 using 來保證資源的釋放

### Entity Framework 規範 (僅適用於現有專案)
1. **DbContext 管理**
   - 使用 dependency injection
   - 適當的連線字串管理
   - 實作適當的 migration 策略

2. **模型設計**
   - 使用適當的資料註解
   - 實作適當的關聯性
   - 使用適當的索引

## 測試規範

### 單元測試
1. **測試結構**
   - 使用 AAA 模式 (Arrange, Act, Assert)
   - 測試方法命名：`MethodName_Scenario_ExpectedResult`
   - 使用適當的測試框架 (NUnit, xUnit, MSTest)

2. **測試覆蓋率**
   - 目標覆蓋率至少 80%
   - 重點測試業務邏輯和關鍵路徑

### 整合測試
1. **API 測試**
   - 測試完整的 API 端點
   - 使用適當的測試資料
   - 測試錯誤情況

## 部署與 CI/CD

### Azure DevOps 規範
1. **Pipeline 配置**
   - 使用 YAML 格式的 pipeline
   - 實作適當的建置和部署階段
   - 使用適當的環境變數

2. **版本控制**
   - 使用 Git Flow 或 GitHub Flow
   - 適當的 commit 訊息格式
   - 使用 Pull Request 進行程式碼審查

## 安全性規範

### 資料保護
1. **敏感資料**
   - 不在程式碼中硬編碼敏感資訊
   - 使用適當的加密機制
   - 實作適當的資料遮罩

2. **輸入驗證**
   - 實作適當的輸入驗證
   - 使用參數化查詢防止 SQL 注入
   - 實作適當的 XSS 防護

3. **套件安全性管理**
   - **定期安全性審查：**
     * 每月至少執行一次 `dotnet list package --vulnerable` 檢查
     * 建立安全性弱點追蹤清單
     * 優先修復 Critical 和 High 等級的弱點
   - **已知高風險套件：**
     * `System.Data.SqlClient` 4.8.1 及更早版本 - 有 RCE 和資訊洩露弱點
     * `Newtonsoft.Json` 12.x 及更早版本 - 有 DoS 弱點
     * 任何顯示在 GitHub Advisory Database 的套件
   - **安全更新流程：**
     1. 識別安全性弱點
     2. 評估影響範圍和嚴重性
     3. 測試套件更新的相容性
     4. 更新程式碼（如需要）
     5. 執行完整測試
     6. 產生更新報告
     7. 部署至測試環境驗證
     8. 部署至正式環境
   - **安全性資源：**
     * GitHub Advisory Database: https://github.com/advisories
     * NVD (National Vulnerability Database): https://nvd.nist.gov/
     * Microsoft Security Response Center: https://msrc.microsoft.com/

## 效能規範

### 最佳化原則
1. **資料庫查詢**
   - 使用適當的索引
   - 避免 N+1 查詢問題
   - 使用適當的查詢優化

2. **快取策略**
   - 實作適當的快取機制
   - 使用適當的快取過期策略
   - 監控快取效能

## 日誌與監控

### 日誌規範
1. **日誌級別**
   - 使用適當的日誌級別 (Debug, Info, Warning, Error, Fatal)
   - 實作結構化日誌
   - 包含適當的上下文資訊

2. **監控**
   - 實作適當的應用程式監控
   - 設定適當的警示機制
   - 監控關鍵業務指標

## 文件規範

### 程式碼文件
1. **XML 文件註解**
   - 為公開 API 提供 XML 文件註解
   - 使用適當的標籤和格式
   - 保持文件與程式碼同步

2. **README 文件**
   - 每個專案應包含 README.md
   - 包含專案概述、安裝說明、使用方式
   - 包含適當的範例程式碼

## 程式碼審查規範

### 審查檢查清單
1. **功能正確性**
   - 程式碼是否實作預期功能
   - 是否處理邊界情況
   - 是否有適當的錯誤處理

2. **程式碼品質**
   - 是否遵循命名規範
   - 是否有適當的註解
   - 是否遵循 SOLID 原則

3. **安全性**
   - 是否有安全漏洞
   - 是否適當處理敏感資料
   - 是否有適當的輸入驗證

## 工具與環境

### 開發工具
1. **IDE 設定**
   - 使用 Visual Studio 或 Visual Studio Code
   - 配置適當的程式碼格式化設定
   - 使用適當的擴充功能

2. **NuGet 套件管理**
   - 使用適當的套件版本
   - 定期更新套件
   - 記錄套件相依性
   - 定期檢查並修復安全性弱點

3. **套件安全性管理規範**
   - 定期掃描專案中的 NuGet 套件安全性弱點
   - 優先處理高嚴重性 (High) 和關鍵 (Critical) 的安全性問題
   - 建立套件更新的審核流程，確保相容性
   - 在更新套件後進行完整的功能測試
   - 記錄每次安全性更新的詳細資訊，包括：
     * 原始套件版本和新版本
     * 修復的安全性弱點編號 (CVE/GHSA)
     * 受影響的專案範圍
     * 程式碼變更清單
   - 產生套件安全性更新報告文件 (.md 格式)

4. **推薦的核心套件版本**
   - **資料存取層：**
     * 使用 `Microsoft.Data.SqlClient` (5.1.x 或更高) 而非 `System.Data.SqlClient`
     * 使用 `Dapper` (2.1.x 或更高) 作為 ORM
   - **JSON 處理：**
     * 使用 `Newtonsoft.Json` (13.0.x 或更高) 或考慮改用 `System.Text.Json`
   - **ASP.NET Core：**
     * 使用與專案目標框架相符的最新穩定版本
     * 範例：.NET 6.0 專案使用 6.0.x 的最新修補版本
   - **避免使用的過時套件：**
     * `System.Data.SqlClient` - 已知安全性弱點，改用 `Microsoft.Data.SqlClient`
     * `Microsoft.AspNet.*` - 舊版 ASP.NET MVC 套件，不適用於 ASP.NET Core
     * 任何標示為 deprecated 或有已知安全弱點的套件

5. **套件更新最佳實踐**
   - 在更新套件前先備份或建立分支
   - 優先更新 patch 版本（如 1.2.3 → 1.2.4）
   - 謹慎更新 minor 版本（如 1.2.x → 1.3.x），需進行完整測試
   - 更新 major 版本（如 1.x → 2.x）需要詳細評估影響並規劃遷移
   - 使用 `dotnet list package --vulnerable` 指令定期檢查弱點
   - 使用 `dotnet list package --outdated` 指令檢查過時套件

### Utility 專案與資源管理規範
1. **Utility 專案優先原則**
   - 在引入函式庫、外部資源、檔案前需要優先參考該解決方案的所有 Utility 專案
   - 檢查 LifeTech.Utility、LifeTech.EC.Web.Utility、LifeTech.EC.API.Utility 等現有工具專案
   - 優先使用現有的 Utility 專案中的功能，避免重複實作
   - 在引入新的外部套件前，先確認是否有現有的 Utility 功能可以替代

2. **wwwroot 專案規範**
   - 如果專案包含 wwwroot 資料夾，優先參考其中的靜態資源
   - 檢查現有的 CSS、JavaScript、圖片等資源是否可重複使用
   - 避免重複建立相同的靜態資源

3. **Utility 專案修改限制**
   - 不可以修改 Utility 專案的程式碼（除非正在對 Utility 專案進行開發修改）
   - 如需修改 Utility 專案，必須明確告知並獲得授權
   - 修改 Utility 專案時需要考慮對其他專案的影響
   - 修改 Utility 專案後需要更新相關的文件和版本號

4. **外部資源引入規範**
   - 引入外部函式庫前必須評估是否有安全風險
   - 優先選擇經過驗證且維護良好的開源專案
   - 記錄所有外部依賴的版本和授權資訊
   - 定期檢查外部依賴的更新和安全修補

## 維護與支援

### 技術債務管理
1. **重構策略**
   - 定期進行程式碼重構
   - 識別和解決技術債務
   - 保持程式碼的可維護性

2. **版本管理**
   - 使用語義化版本控制
   - 適當的版本標記
   - 維護變更日誌

---

## 注意事項

1. 本規則文件應定期更新以反映最新的最佳實務
2. 所有開發人員應熟悉並遵循這些規範
3. 在特殊情況下，可與團隊討論例外情況
4. 持續改進和優化這些規範

## 聯絡資訊

如有任何問題或建議，請聯絡開發團隊負責人。

---

## 更新歷史

### 版本 1.6 (2025年10月28日)
- 新增「套件安全性管理規範」章節
- 新增「推薦的核心套件版本」指南
- 新增「套件更新最佳實踐」規範
- 新增「SQL Client 套件規範」，強制使用 Microsoft.Data.SqlClient
- 詳細說明 System.Data.SqlClient 遷移至 Microsoft.Data.SqlClient 的流程
- 強化套件安全性檢查和更新流程

### 版本 1.5 (2025年9月26日)
- 初始版本建立

---

*最後更新：2025年10月28日*
*版本：1.6*

---

## 附錄：版本更新詳細說明

### 🆕 版本 1.6 新增內容摘要

#### 1. 套件安全性管理規範（工具與環境章節）
新增完整的 NuGet 套件安全性管理流程，包括：
- 定期掃描套件安全性弱點
- 安全性問題優先級處理原則
- 套件更新審核流程
- 詳細的更新記錄要求
- 強制產生安全性更新報告

#### 2. 推薦的核心套件版本
明確指定各類型套件的推薦版本：
- **資料存取層：** Microsoft.Data.SqlClient 5.1.x+, Dapper 2.1.x+
- **JSON 處理：** Newtonsoft.Json 13.0.x+
- **ASP.NET Core：** 與目標框架相符的最新穩定版本
- **明確列出避免使用的過時套件**

#### 3. 套件更新最佳實踐
提供實用的套件更新指南：
- 版本更新策略（patch/minor/major）
- 更新前的備份建議
- 使用 dotnet CLI 工具檢查弱點和過時套件
- 完整的測試流程

#### 4. SQL Client 套件規範（資料庫規範章節）
專門針對 SQL Client 套件的詳細規範：
- **強制使用** Microsoft.Data.SqlClient
- **禁止使用** System.Data.SqlClient
- 詳細的遷移指南
- 安全性考量說明
- API 相容性說明

#### 5. 套件安全性管理（安全性規範章節）
在安全性章節新增套件管理內容：
- 定期安全性審查流程
- 已知高風險套件清單
- 8 步驟安全更新流程
- 安全性資源連結

---

### ✅ 主要變更摘要

#### 明確的套件政策
- 從「建議」提升為「強制」使用 Microsoft.Data.SqlClient
- 列出禁止使用的過時套件清單
- 建立清晰的套件版本要求

#### 完整的安全性流程
- 定義月度安全性檢查機制
- 建立優先級處理原則（Critical > High > Medium > Low）
- 8 步驟安全更新流程
- 強制產生更新報告文件

#### 實用的工具和指令
- `dotnet list package --vulnerable` - 檢查弱點
- `dotnet list package --outdated` - 檢查過時套件
- 提供安全性資源連結

#### 遷移指南
- System.Data.SqlClient → Microsoft.Data.SqlClient 完整遷移步驟
- 命名空間變更說明
- API 相容性保證

---

### 📋 與實際更新的對應

本次 `.cursorrules` 更新直接基於 ERP.Web 專案套件安全性更新：

| 實際執行內容 | .cursorrules 新增規範 |
|------------|-------------------|
| 更新 Newtonsoft.Json 12.0.3 → 13.0.3 | 推薦套件版本：Newtonsoft.Json 13.0.x+ |
| 更新 System.Data.SqlClient → Microsoft.Data.SqlClient | SQL Client 套件規範（強制使用） |
| 更新 6 個檔案的 using 語句 | 遷移指南：命名空間變更說明 |
| 產生「套件安全性更新報告.md」 | 強制產生安全性更新報告 |
| 解決 GHSA 安全性弱點 | 已知高風險套件清單 |

---

### 🎯 規範實施影響

#### 對新專案
- 必須使用 Microsoft.Data.SqlClient
- 必須使用 Newtonsoft.Json 13.0.x+
- 必須定期執行安全性檢查

#### 對現有專案
- 建議盡快遷移至 Microsoft.Data.SqlClient
- 建議更新過時的套件版本
- 建立月度安全性檢查排程

#### 對開發流程
- 套件更新需要產生報告
- 安全性弱點需要優先處理
- 更新前需要完整測試

---

### 📚 後續建議

#### 立即行動
1. ✅ 已完成：ERP.Web 專案套件安全性更新
2. ✅ 已完成：.cursorrules 規範更新
3. ⏳ 建議：將新規範通知開發團隊
4. ⏳ 建議：在其他 LifeTech 專案中執行安全性檢查

#### 定期行動
1. 📅 每月第一週：執行 `dotnet list package --vulnerable`
2. 📅 每季：檢查並更新過時套件
3. 📅 每半年：審查和更新 .cursorrules 規範

#### 改進方向
1. 考慮整合自動化安全性掃描工具
2. 建立 CI/CD Pipeline 自動檢查弱點
3. 建立套件白名單和黑名單
4. 定期舉辦安全性培訓

---

### 🔗 參考資源

#### 官方文件
- [Microsoft.Data.SqlClient 文件](https://docs.microsoft.com/en-us/sql/connect/ado-net/introduction-microsoft-data-sqlclient-namespace)
- [.NET 套件安全性](https://docs.microsoft.com/en-us/nuget/concepts/security-best-practices)

#### 安全性資源
- [GitHub Advisory Database](https://github.com/advisories)
- [NVD - National Vulnerability Database](https://nvd.nist.gov/)
- [Microsoft Security Response Center](https://msrc.microsoft.com/)

#### 本專案相關文件
- `套件安全性報告.md` - 套件安全性更新報告和遷移指南
- `系統設定調整說明.md` - 權限控制系統使用和實作說明

---

### 📝 總結

本次 `.cursorrules` 更新（版本 1.6）大幅強化了套件安全性管理規範，從「建議」提升為「強制」要求，並提供完整的實施指南。這些規範基於實際的安全性更新經驗，確保所有 LifeTech 專案都能維持高水準的安全性標準。

**關鍵改變：**
- ✅ 強制使用安全的套件版本
- ✅ 建立定期安全性檢查機制
- ✅ 提供完整的遷移和更新指南
- ✅ 強化文件化要求

---

**規範版本：** 1.6  
**附錄更新時間：** 2025年10月28日  
**文件整合執行者：** Cursor AI Assistant

