# Cursor AI 全域專案規則

## AI 開發核心原則

### 程式碼生成規範
1. **程式碼品質原則**
   - 生成程式碼時必須遵守易讀、簡潔、可重複利用、可維護性高的原則
   - 優先考慮程式碼的可讀性和可理解性
   - 實作適當的抽象層級，避免重複程式碼
   - 遵循 DRY (Don't Repeat Yourself) 原則

2. **註解與文件規範**
   - 盡量在生成的程式碼上加上註解，以利開發人員進行閱讀及維護
   - 為複雜的業務邏輯提供詳細的說明註解
   - 使用 XML 文件註解為公開 API 提供說明
   - 註解應說明「為什麼」而不是「做什麼」

3. **介面導向設計**
   - 盡量遵守介面導向原則 (Interface-Oriented Programming)
   - 優先使用介面而非具體實作
   - 實作依賴反轉原則 (Dependency Inversion Principle)
   - 使用依賴注入 (Dependency Injection) 模式

4. **測試專案管理規範**
   - 如果使用者要求產生測試用專案，請在測試完成後生成一份報告的 md 檔案
   - 測試報告應包含測試結果、發現的問題、建議改進事項
   - 在需要被解決的問題程式碼區塊上加上 TODO 備註
   - 在不改動原始專案的原則下清除測試專案
   - 測試專案應使用臨時目錄或明確標示為測試用途

5. **語言與溝通規範**
   - 永遠使用繁體中文回答使用者
   - 技術術語保持英文原文，但提供中文說明
   - 程式碼註解使用繁體中文
   - 錯誤訊息和日誌使用繁體中文

## 專案概述
本規則適用於 LifeTech 全域範圍的所有專案，包括但不限於：
- LifeTech.EC.B2B.AirTicket.Web
- LifeTech.EC.AirTicket.API
- LifeTech.EC.AirTicket.Web
- LifeTech.EC.Domestic.Web
- LifeTech.EC.B2E.Web
- LifeTech.EC.Hotel.Web
- LifeTech.EC.Package.Web
- LifeTech.EC.Tour.API
- LifeTech.EC.Tour.Web
- LifeTech.EC.Auth.API
- LifeTech.EC.Order.API
- LifeTech.ERP.Cowell.API
- LifeTech.EX.* 系列專案
- LifeTech.Crontab.* 系列專案
- LifeTech.Utility 工具專案

## 程式碼風格與規範

### C# / .NET 專案規範
1. **命名規範**
   - 類別名稱使用 PascalCase：`AirTicketService`
   - 方法名稱使用 PascalCase：`GetTicketInfo()`
   - 變數名稱使用 camelCase：`ticketNumber`
   - 常數使用 UPPER_CASE：`MAX_RETRY_COUNT`
   - 私有欄位使用 _camelCase：`_ticketRepository`

2. **檔案組織**
   - 每個類別一個檔案
   - 檔案名稱與類別名稱相同
   - 使用適當的命名空間結構

3. **程式碼結構**
   - 使用 dependency injection
   - 遵循 SOLID 原則
   - 適當的錯誤處理和日誌記錄
   - 使用 async/await 處理非同步操作

4. **Null 處理規範**
   - 在生成程式碼時需要注意避免 null 會發生的 ReferenceToNull 例外
   - 應該對可能為 null 的物件進行預防性檢查
   - 可以使用 if 判斷、?. 空條件運算子 (null-conditional operator) 等
   - 依然需要保持簡潔及易讀原則
   - 優先使用空條件運算子 (?.) 和空合併運算子 (??) 來簡化 null 檢查
   - 對於集合類型，使用 null 檢查後再進行操作
   - 在方法參數驗證中使用 null 檢查，避免傳入 null 值

### Web 專案規範 (ASP.NET MVC/Web API)
1. **控制器規範**
   - 控制器名稱以 Controller 結尾
   - 使用適當的 HTTP 動詞屬性
   - 實作適當的錯誤處理

2. **視圖規範**
   - 使用 Razor 語法
   - 保持視圖簡潔，複雜邏輯移至控制器或服務層
   - 使用適當的 HTML 語義標籤

3. **模型規範**
   - 使用 Data Annotations 進行驗證
   - 實作適當的 ViewModel 模式
   - 使用強型別模型

### API 專案規範
1. **RESTful API 設計**
   - 使用適當的 HTTP 狀態碼
   - 實作統一的回應格式
   - 使用適當的 HTTP 動詞

2. **安全性**
   - 實作適當的認證和授權
   - 使用 HTTPS
   - 實作適當的 CORS 政策

## 資料庫規範

### ORM 與資料存取規範
1. **ORM 選擇**
   - 連線資料庫需要使用 Dapper 作為 ORM
   - 避免使用 Entity Framework，除非專案已有現有的 EF 實作
   - 使用 Dapper 的 Query 和 Execute 方法進行資料操作
   - 實作適當的 Repository 模式封裝 Dapper 操作

2. **SQL 語法規範**
   - SQL 語法需要使用 SQL Server 2016 版本能夠執行的語法
   - 避免使用 SQL Server 2016 不支援的新功能
   - 確保 SQL 語法向後相容性
   - 使用標準的 T-SQL 語法，避免版本特定的擴展功能

3. **SQL 效能與可讀性**
   - SQL 語法生成時需要考慮到最佳化效能以及易讀性
   - 在需要的地方加上註解說明複雜的查詢邏輯
   - 使用適當的索引提示 (Index Hints) 優化查詢效能
   - 避免 SELECT * 語法，明確指定需要的欄位
   - 使用適當的 JOIN 語法而非子查詢（當效能允許時）
   - 為複雜的 WHERE 條件加上註解說明

4. **SQL 執行限制**
   - Cursor AI Agent 並沒有執行 T-SQL 語法的權限
   - 所有 SQL 語法僅供參考，需要開發人員手動執行
   - 提供 SQL 語法時應包含完整的執行說明
   - 建議在測試環境中先驗證 SQL 語法的正確性

5. **Transaction 交易管理規範**
   - 在使用 .NET 提供的 Transaction 時，需要注意避免分散式交易
   - 需要保持裡面的每個資料庫連線使用相同的連線字串
   - 使用 TransactionScope 時，確保所有資料庫操作使用相同的連線
   - 避免在 TransactionScope 內使用不同資料庫或不同連線字串
   - 優先使用 SqlTransaction 而非 TransactionScope（當只涉及單一資料庫時）
   - 在需要跨多個資料庫操作時，考慮使用 Saga 模式或其他分散式交易替代方案
   - 適當處理交易失敗的 rollback 機制
   - 為了保持易讀性，盡量使用 .NET 提供的 Transaction 功能
   - 在連線資料庫時需要使用 using 來保證資源的釋放

### Entity Framework 規範 (僅適用於現有專案)
1. **DbContext 管理**
   - 使用 dependency injection
   - 適當的連線字串管理
   - 實作適當的 migration 策略

2. **模型設計**
   - 使用適當的資料註解
   - 實作適當的關聯性
   - 使用適當的索引

## 測試規範

### 單元測試
1. **測試結構**
   - 使用 AAA 模式 (Arrange, Act, Assert)
   - 測試方法命名：`MethodName_Scenario_ExpectedResult`
   - 使用適當的測試框架 (NUnit, xUnit, MSTest)

2. **測試覆蓋率**
   - 目標覆蓋率至少 80%
   - 重點測試業務邏輯和關鍵路徑

### 整合測試
1. **API 測試**
   - 測試完整的 API 端點
   - 使用適當的測試資料
   - 測試錯誤情況

## 部署與 CI/CD

### Azure DevOps 規範
1. **Pipeline 配置**
   - 使用 YAML 格式的 pipeline
   - 實作適當的建置和部署階段
   - 使用適當的環境變數

2. **版本控制**
   - 使用 Git Flow 或 GitHub Flow
   - 適當的 commit 訊息格式
   - 使用 Pull Request 進行程式碼審查

## 安全性規範

### 資料保護
1. **敏感資料**
   - 不在程式碼中硬編碼敏感資訊
   - 使用適當的加密機制
   - 實作適當的資料遮罩

2. **輸入驗證**
   - 實作適當的輸入驗證
   - 使用參數化查詢防止 SQL 注入
   - 實作適當的 XSS 防護

## 效能規範

### 最佳化原則
1. **資料庫查詢**
   - 使用適當的索引
   - 避免 N+1 查詢問題
   - 使用適當的查詢優化

2. **快取策略**
   - 實作適當的快取機制
   - 使用適當的快取過期策略
   - 監控快取效能

## 日誌與監控

### 日誌規範
1. **日誌級別**
   - 使用適當的日誌級別 (Debug, Info, Warning, Error, Fatal)
   - 實作結構化日誌
   - 包含適當的上下文資訊

2. **監控**
   - 實作適當的應用程式監控
   - 設定適當的警示機制
   - 監控關鍵業務指標

## 文件規範

### 程式碼文件
1. **XML 文件註解**
   - 為公開 API 提供 XML 文件註解
   - 使用適當的標籤和格式
   - 保持文件與程式碼同步

2. **README 文件**
   - 每個專案應包含 README.md
   - 包含專案概述、安裝說明、使用方式
   - 包含適當的範例程式碼

## 程式碼審查規範

### 審查檢查清單
1. **功能正確性**
   - 程式碼是否實作預期功能
   - 是否處理邊界情況
   - 是否有適當的錯誤處理

2. **程式碼品質**
   - 是否遵循命名規範
   - 是否有適當的註解
   - 是否遵循 SOLID 原則

3. **安全性**
   - 是否有安全漏洞
   - 是否適當處理敏感資料
   - 是否有適當的輸入驗證

## 工具與環境

### 開發工具
1. **IDE 設定**
   - 使用 Visual Studio 或 Visual Studio Code
   - 配置適當的程式碼格式化設定
   - 使用適當的擴充功能

2. **NuGet 套件管理**
   - 使用適當的套件版本
   - 定期更新套件
   - 記錄套件相依性

### Utility 專案與資源管理規範
1. **Utility 專案優先原則**
   - 在引入函式庫、外部資源、檔案前需要優先參考該解決方案的所有 Utility 專案
   - 檢查 LifeTech.Utility、LifeTech.EC.Web.Utility、LifeTech.EC.API.Utility 等現有工具專案
   - 優先使用現有的 Utility 專案中的功能，避免重複實作
   - 在引入新的外部套件前，先確認是否有現有的 Utility 功能可以替代

2. **wwwroot 專案規範**
   - 如果專案包含 wwwroot 資料夾，優先參考其中的靜態資源
   - 檢查現有的 CSS、JavaScript、圖片等資源是否可重複使用
   - 避免重複建立相同的靜態資源

3. **Utility 專案修改限制**
   - 不可以修改 Utility 專案的程式碼（除非正在對 Utility 專案進行開發修改）
   - 如需修改 Utility 專案，必須明確告知並獲得授權
   - 修改 Utility 專案時需要考慮對其他專案的影響
   - 修改 Utility 專案後需要更新相關的文件和版本號

4. **外部資源引入規範**
   - 引入外部函式庫前必須評估是否有安全風險
   - 優先選擇經過驗證且維護良好的開源專案
   - 記錄所有外部依賴的版本和授權資訊
   - 定期檢查外部依賴的更新和安全修補

## 維護與支援

### 技術債務管理
1. **重構策略**
   - 定期進行程式碼重構
   - 識別和解決技術債務
   - 保持程式碼的可維護性

2. **版本管理**
   - 使用語義化版本控制
   - 適當的版本標記
   - 維護變更日誌

---

## 注意事項

1. 本規則文件應定期更新以反映最新的最佳實務
2. 所有開發人員應熟悉並遵循這些規範
3. 在特殊情況下，可與團隊討論例外情況
4. 持續改進和優化這些規範

## 聯絡資訊

如有任何問題或建議，請聯絡開發團隊負責人。

---

*最後更新：2025年9月26日*
*版本：1.5*

