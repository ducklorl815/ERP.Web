# ERP.Web 系統設定調整說明

## 📋 文件概述

本文件整合了 ERP.Web 專案的系統設定調整說明，包括：
- 權限控制系統使用指南
- 權限控制系統實作摘要
- 實際應用範例與測試步驟

---

# 第一部分：權限控制系統使用說明

## 📋 概述

本系統提供完整的權限檢查機制,讓頁面能夠根據使用者權限自動顯示或隱藏功能按鈕/連結。

**核心元件：**
1. **IPermissionService** - 權限檢查服務介面
2. **PermissionService** - 權限檢查服務實作（含快取機制）
3. **PermissionTagHelper** - 自訂 TagHelper，在 View 中方便地檢查權限

---

## 🚀 快速開始

### 步驟 1：服務已註冊（已完成）

在 `Program.cs` 中已註冊：
```csharp
builder.Services.AddMemoryCache();
builder.Services.AddHttpContextAccessor();
builder.Services.AddSingleton<IPermissionService, PermissionService>();
```

### 步驟 2：TagHelper 已啟用（已完成）

在 `_ViewImports.cshtml` 中已添加：
```cshtml
@addTagHelper *, ERP.Web.Utility
```

### 步驟 3：在 View 中使用權限檢查

---

## 💡 使用方式

### 方式 1：完整版（推薦用於複雜場景）

適用於需要明確指定 Controller 和 Action 的場景：

```cshtml
<div permission-controller="ControllerSetting" permission-action="TreeView">
    <a asp-action="TreeView" asp-controller="ControllerSetting">
        <span class="float-right" style="cursor:pointer;">
            <i class="fa-solid fa-plus"></i> 新增群組
        </span>
    </a>
</div>
```

**特點：**
- 明確指定 Controller 和 Action
- 適合需要條件判斷的複雜場景
- 可以包裹多個元素

---

### 方式 2：簡化版（推薦用於簡單場景）

適用於單一連結或按鈕：

```cshtml
<a permission-check="ControllerSetting.TreeView" 
   asp-action="TreeView" 
   asp-controller="ControllerSetting">
    <i class="fa-solid fa-plus"></i> 新增群組
</a>
```

**特點：**
- 簡潔易讀
- 格式：`Controller.Action`
- 適合單一元素

---

### 方式 3：包裹多個元素

當多個元素需要相同權限時：

```cshtml
<div permission-check="ControllerSetting.DeleteAccessGroup">
    <button onclick="DeleteAccessGroup('@item.ID')" class="btn btn-danger btn-sm">
        <i class="fa fa-trash"></i> 刪除
    </button>
    <button onclick="EditAccessGroup('@item.ID')" class="btn btn-warning btn-sm">
        <i class="fa fa-edit"></i> 編輯
    </button>
</div>
```

---

## 🔧 進階選項

### 保留元素但禁用

預設情況下，沒有權限時會完全移除元素。如果您希望保留元素但禁用：

```cshtml
<button permission-check="ControllerSetting.TreeView" 
        permission-remove="false"
        class="btn btn-primary">
    新增群組
</button>
```

**效果：**
- 沒有權限時：元素保留但添加 `disabled` 屬性和 `display:none` 樣式
- 有權限時：正常顯示

---

## 📝 實際範例

### 範例 1：AccessGroupSearchList.cshtml（修改後）

**修改前：**
```cshtml
<a asp-action="TreeView" asp-controller="ControllerSetting">
    <span class="float-right" style="cursor:pointer;">
        <i class="fa-solid fa-plus"></i> 新增群組
    </span>
</a>
```

**修改後：**
```cshtml
<!-- 方法 A：包裹整個連結 -->
<div permission-check="ControllerSetting.TreeView">
    <a asp-action="TreeView" asp-controller="ControllerSetting">
        <span class="float-right" style="cursor:pointer;">
            <i class="fa-solid fa-plus"></i> 新增群組
        </span>
    </a>
</div>

<!-- 方法 B：直接在連結上使用 -->
<a permission-check="ControllerSetting.TreeView"
   asp-action="TreeView" 
   asp-controller="ControllerSetting">
    <span class="float-right" style="cursor:pointer;">
        <i class="fa-solid fa-plus"></i> 新增群組
    </span>
</a>
```

---

### 範例 2：表格中的操作按鈕

```cshtml
<table class="table">
    @foreach (var item in Model.List)
    {
        <tr>
            <td>@item.GroupName</td>
            <td>@item.GroupDesc</td>
            <td>
                <!-- 編輯按鈕 -->
                <a permission-check="ControllerSetting.EditAccessGroup"
                   asp-action="EditAccessGroup" 
                   asp-route-id="@item.ID"
                   class="btn btn-warning btn-sm">
                    <i class="fa fa-edit"></i> 編輯
                </a>
                
                <!-- 刪除按鈕 -->
                <button permission-check="ControllerSetting.DeleteAccessGroup"
                        onclick="DeleteAccessGroup('@item.ID')" 
                        class="btn btn-danger btn-sm">
                    <i class="fa fa-trash"></i> 刪除
                </button>
                
                <!-- 查看按鈕（總是顯示，不檢查權限） -->
                <a asp-action="ViewAccessGroup" 
                   asp-route-id="@item.ID"
                   class="btn btn-info btn-sm">
                    <i class="fa fa-eye"></i> 查看
                </a>
            </td>
        </tr>
    }
</table>
```

---

### 範例 3：搜尋表單中的按鈕

```cshtml
<form method="get">
    <div class="row">
        <div class="col">
            <input asp-for="Keyword.GroupName" class="form-control" />
        </div>
        <div class="col">
            <!-- 搜尋按鈕（通常不需要權限檢查） -->
            <button type="submit" class="btn btn-success">
                <i class="fa fa-search"></i> 搜尋
            </button>
            
            <!-- 進階搜尋（需要特殊權限） -->
            <button permission-check="ControllerSetting.AdvancedSearch"
                    type="button" 
                    class="btn btn-info">
                <i class="fa fa-filter"></i> 進階搜尋
            </button>
            
            <!-- 匯出（需要權限） -->
            <button permission-check="ControllerSetting.ExportData"
                    type="button" 
                    class="btn btn-primary"
                    onclick="ExportData()">
                <i class="fa fa-download"></i> 匯出
            </button>
        </div>
    </div>
</form>
```

---

### 範例 4：導覽選單

```cshtml
<ul class="nav">
    <!-- 首頁（不需要權限） -->
    <li><a asp-action="Index">首頁</a></li>
    
    <!-- 需要權限的選單項目 -->
    <li permission-check="ControllerSetting.AccessGroupSearchList">
        <a asp-action="AccessGroupSearchList">群組管理</a>
    </li>
    
    <li permission-check="ControllerSetting.ControllerSearchList">
        <a asp-action="ControllerSearchList">功能管理</a>
    </li>
    
    <li permission-check="Admin.UserManagement">
        <a asp-controller="Admin" asp-action="UserManagement">使用者管理</a>
    </li>
</ul>
```

---

## 🎯 Controller Action 命名規範

為了正確檢查權限，請確保：

1. **Controller 名稱**：不包含 "Controller" 後綴
   - ✅ 正確：`ControllerSetting`
   - ❌ 錯誤：`ControllerSettingController`

2. **Action 名稱**：使用實際的 Action 方法名稱
   - ✅ 正確：`TreeView`、`DeleteAccessGroup`
   - ❌ 錯誤：`tree-view`、`delete_access_group`

3. **格式一致性**：
   ```cshtml
   <!-- 這兩者必須一致 -->
   <a permission-check="ControllerSetting.TreeView"
      asp-action="TreeView" 
      asp-controller="ControllerSetting">
   ```

---

## 🔍 權限檢查邏輯

### 檢查流程

1. **取得使用者資訊**
   - 從 `HttpContext.User.Identity.Name` 取得當前使用者

2. **查詢權限清單**
   - 從資料庫查詢使用者所屬的權限群組
   - 展開樹狀結構取得所有 Controller ID
   - 查詢對應的 Controller 和 Action

3. **快取機制**
   - 權限清單會快取 30 分鐘
   - 減少資料庫查詢，提升效能
   - 滑動過期時間：10 分鐘無存取則清除

4. **權限比對**
   - 格式：`Controller.Action`（不區分大小寫）
   - 例如：`controllersetting.treeview`

---

## 🛠️ 在 Controller 中使用權限服務

除了在 View 中使用 TagHelper，您也可以在 Controller 中直接使用權限服務：

```csharp
using ERP.Web.Utility.Services;
using Microsoft.AspNetCore.Mvc;

public class ControllerSettingController : Controller
{
    private readonly IPermissionService _permissionService;

    public ControllerSettingController(IPermissionService permissionService)
    {
        _permissionService = permissionService;
    }

    public async Task<IActionResult> AccessGroupSearchList()
    {
        // 檢查使用者是否有「新增群組」的權限
        var canAddGroup = await _permissionService.HasPermissionAsync(
            User.Identity.Name, 
            "ControllerSetting", 
            "TreeView"
        );

        ViewBag.CanAddGroup = canAddGroup;

        return View();
    }

    [HttpPost]
    public async Task<IActionResult> DeleteAccessGroup(Guid id)
    {
        // 檢查權限
        var hasPermission = await _permissionService.HasPermissionAsync(
            User.Identity.Name, 
            "ControllerSetting", 
            "DeleteAccessGroup"
        );

        if (!hasPermission)
        {
            return Forbid(); // 403 Forbidden
        }

        // 執行刪除邏輯...
        return Ok();
    }
}
```

### 取得使用者所有權限

```csharp
public async Task<IActionResult> GetUserPermissions()
{
    var permissions = await _permissionService.GetUserPermissionsAsync(User.Identity.Name);
    
    // permissions 是 HashSet<string>
    // 包含格式如：["controllersetting.treeview", "controllersetting.delete", ...]
    
    return Json(permissions);
}
```

---

## 🎨 最佳實踐

### 1. 選擇合適的使用方式

**使用 TagHelper（View）：**
- ✅ 單純的顯示/隱藏邏輯
- ✅ 不影響業務邏輯
- ✅ 減少 Controller 程式碼

**使用 Service（Controller）：**
- ✅ 需要在業務邏輯中判斷權限
- ✅ 需要回傳不同的結果
- ✅ 需要記錄權限檢查日誌

### 2. 權限細粒度

建議為每個重要操作設定獨立的權限：
- ✅ `ControllerSetting.Create` - 新增
- ✅ `ControllerSetting.Edit` - 編輯
- ✅ `ControllerSetting.Delete` - 刪除
- ✅ `ControllerSetting.View` - 查看
- ✅ `ControllerSetting.Export` - 匯出

### 3. 快取管理

權限變更後需要清除快取：

```csharp
public class PermissionManagementController : Controller
{
    private readonly IPermissionService _permissionService;

    [HttpPost]
    public async Task<IActionResult> UpdateUserPermission(string userName)
    {
        // 更新權限邏輯...
        
        // 清除該使用者的權限快取
        if (_permissionService is PermissionService permissionService)
        {
            permissionService.ClearUserPermissionsCache(userName);
        }
        
        return Ok();
    }
}
```

### 4. 效能考量

- ✅ 權限服務使用 Singleton 生命週期
- ✅ 內建記憶體快取（30分鐘）
- ✅ 滑動過期機制（10分鐘）
- ✅ 一次查詢，多次使用

---

## 🐛 故障排除

### 問題 1：TagHelper 不生效

**檢查清單：**
1. ✅ 確認 `_ViewImports.cshtml` 中有 `@addTagHelper *, ERP.Web.Utility`
2. ✅ 確認 `Program.cs` 中已註冊服務
3. ✅ 重新建置專案（`dotnet build`）
4. ✅ 清除瀏覽器快取

### 問題 2：所有元素都被隱藏

**可能原因：**
1. 使用者未登入（`User.Identity.Name` 為 null）
2. 使用者沒有任何權限
3. Controller/Action 名稱不符合

**解決方式：**
```cshtml
<!-- 調試用：顯示當前使用者 -->
<p>當前使用者：@User.Identity.Name</p>
<p>是否已認證：@User.Identity.IsAuthenticated</p>
```

### 問題 3：權限更新後沒有立即生效

**原因：** 快取機制

**解決方式：**
1. 等待快取過期（最多 30 分鐘）
2. 或在權限更新後清除快取（見上方「快取管理」）
3. 或重新啟動應用程式

### 問題 4：格式錯誤

**常見錯誤：**
```cshtml
<!-- ❌ 錯誤：包含 Controller 後綴 -->
<div permission-check="ControllerSettingController.TreeView">

<!-- ❌ 錯誤：使用分隔符號 -->
<div permission-check="ControllerSetting/TreeView">

<!-- ❌ 錯誤：使用中文 -->
<div permission-check="控制器設定.樹狀檢視">

<!-- ✅ 正確 -->
<div permission-check="ControllerSetting.TreeView">
```

---

## 📚 API 參考

### IPermissionService 方法

```csharp
// 檢查使用者是否有特定權限
Task<bool> HasPermissionAsync(string userName, string controller, string action);

// 取得使用者所有權限
Task<HashSet<string>> GetUserPermissionsAsync(string userName);

// 根據 ID 檢查權限
Task<bool> HasPermissionByIdAsync(string userName, Guid controllerMainID);
```

### TagHelper 屬性

```cshtml
<!-- 完整版 -->
permission-controller="ControllerName"    <!-- Controller 名稱 -->
permission-action="ActionName"            <!-- Action 名稱 -->

<!-- 簡化版 -->
permission-check="Controller.Action"      <!-- Controller.Action 格式 -->

<!-- 選項 -->
permission-remove="true|false"            <!-- 是否完全移除元素（預設 true） -->
```

---

## 🔐 安全性考量

1. **前端檢查不能替代後端驗證**
   - TagHelper 只控制顯示
   - Controller 中仍需驗證權限

2. **敏感操作必須雙重驗證**
   ```cshtml
   <!-- View 層 -->
   <button permission-check="Admin.DeleteUser">刪除</button>
   ```
   
   ```csharp
   // Controller 層
   [HttpPost]
   public async Task<IActionResult> DeleteUser(Guid id)
   {
       if (!await _permissionService.HasPermissionAsync(User.Identity.Name, "Admin", "DeleteUser"))
           return Forbid();
       
       // 執行刪除...
   }
   ```

3. **權限資料的快取時間**
   - 預設 30 分鐘
   - 根據安全性需求調整

---

# 第二部分：權限控制系統實作摘要

## 📋 實作日期
2025年10月28日

---

## ✅ 已完成的工作

### 1. 核心服務建立

#### ✅ IPermissionService.cs
**位置：** `ERP.Web.Utility/Services/IPermissionService.cs`

**功能：**
- 定義權限檢查服務介面
- 提供三種權限檢查方法

**主要方法：**
```csharp
// 檢查使用者是否有特定 Controller.Action 的權限
Task<bool> HasPermissionAsync(string userName, string controller, string action);

// 取得使用者的所有權限清單
Task<HashSet<string>> GetUserPermissionsAsync(string userName);

// 根據 ID 檢查權限
Task<bool> HasPermissionByIdAsync(string userName, Guid controllerMainID);
```

---

#### ✅ PermissionService.cs
**位置：** `ERP.Web.Utility/Services/PermissionService.cs`

**功能：**
- 實作 IPermissionService 介面
- 內建記憶體快取機制（30分鐘絕對過期，10分鐘滑動過期）
- 與現有的 `ControllerUtilityRepo` 整合
- 自動處理樹狀權限結構

**快取策略：**
- 快取 Key：`UserPermissions_{userName}`
- 絕對過期：30 分鐘
- 滑動過期：10 分鐘（無存取則清除）

**效能優化：**
- 一次查詢，多次使用
- 減少資料庫負載
- 支援清除快取（權限變更時）

---

#### ✅ PermissionTagHelper.cs
**位置：** `ERP.Web.Utility/TagHelpers/PermissionTagHelper.cs`

**功能：**
- 自訂 TagHelper，在 View 中方便檢查權限
- 支援兩種使用方式（完整版和簡化版）
- 可選擇移除元素或僅禁用

---

### 2. 系統整合

#### ✅ Program.cs
**已註冊服務：**
```csharp
builder.Services.AddMemoryCache();
builder.Services.AddHttpContextAccessor();
builder.Services.AddSingleton<IPermissionService, PermissionService>();
```

#### ✅ _ViewImports.cshtml
**已啟用 TagHelper：**
```cshtml
@addTagHelper *, ERP.Web.Utility
```

---

### 3. View 更新

#### ✅ AccessGroupSearchList.cshtml
- 第 16-22 行：新增群組按鈕添加權限檢查
- 權限：`ControllerSetting.TreeView`

#### ✅ _AccessGroupSearchList.cshtml
- 第 18-20 行：編輯連結（群組名稱）添加權限檢查
- 第 32-35 行：刪除按鈕添加權限檢查
- 權限：`ControllerSetting.TreeDataMaintain`、`ControllerSetting.DeleteAccessGroup`

#### ✅ ControllerSettingSearchList.cshtml
- 第 16-22 行：新增Controller按鈕添加權限檢查
- 權限：`ControllerSetting.ControllerCreate`

#### ✅ _ControllerSettingSearchList.cshtml
- 編輯連結和刪除按鈕添加權限檢查
- 權限：`ControllerSetting.ActDataMaintain`、`ControllerSetting.DeleteController`

---

## 🎯 權限清單

### ControllerSetting Controller

| 功能 | 權限名稱 | 說明 |
|------|---------|------|
| 群組搜尋頁 | `ControllerSetting.AccessGroupSearchList` | 查看群組清單 |
| 新增群組 | `ControllerSetting.TreeView` | 建立新的權限群組 |
| 編輯群組 | `ControllerSetting.TreeDataMaintain` | 修改群組設定 |
| 刪除群組 | `ControllerSetting.DeleteAccessGroup` | 刪除權限群組 |
| Controller搜尋頁 | `ControllerSetting.ControllerSettingSearchList` | 查看功能清單 |
| 新增Controller | `ControllerSetting.ControllerCreate` | 建立新功能 |
| 編輯Controller | `ControllerSetting.ActDataMaintain` | 修改功能設定 |
| 刪除Controller | `ControllerSetting.DeleteController` | 刪除功能 |

---

## 📦 檔案清單

### 新建檔案
1. ✅ `ERP.Web.Utility/Services/IPermissionService.cs`
2. ✅ `ERP.Web.Utility/Services/PermissionService.cs`
3. ✅ `ERP.Web.Utility/TagHelpers/PermissionTagHelper.cs`

### 修改檔案
1. ✅ `ERP.Web/Program.cs` - 註冊服務
2. ✅ `ERP.Web/Views/_ViewImports.cshtml` - 啟用 TagHelper
3. ✅ `ERP.Web/Views/ControllerSetting/AccessGroupSearchList.cshtml`
4. ✅ `ERP.Web/Views/ControllerSetting/_AccessGroupSearchList.cshtml`
5. ✅ `ERP.Web/Views/ControllerSetting/ControllerSettingSearchList.cshtml`
6. ✅ `ERP.Web/Views/ControllerSetting/_ControllerSettingSearchList.cshtml`

---

## 🔄 工作流程

### 使用者請求頁面
```
1. 使用者開啟頁面（例如：AccessGroupSearchList）
   ↓
2. LeftSidebarViewComponent 載入（顯示左側選單）
   - 查詢使用者權限
   - 建立選單樹狀結構
   - 標記當前頁面
   ↓
3. 頁面主體載入
   ↓
4. PermissionTagHelper 執行
   - 對每個帶 permission-check 的元素
   - 呼叫 PermissionService.HasPermissionAsync()
   - 判斷是否顯示/隱藏元素
   ↓
5. 頁面渲染完成
   - 有權限的功能顯示
   - 沒有權限的功能隱藏
```

### 權限檢查流程
```
1. TagHelper 觸發權限檢查
   ↓
2. PermissionService.HasPermissionAsync()
   ↓
3. 檢查快取
   - 有快取 → 直接返回結果
   - 無快取 → 執行步驟 4
   ↓
4. 從資料庫查詢
   - ControllerUtilityRepo.GetBoundAccessGroupData() - 取得群組節點
   - ControllerUtilityRepo.GetMenuDataAsync() - 取得選單資料
   ↓
5. 建立權限清單（Controller.Action格式）
   ↓
6. 快取結果（30分鐘）
   ↓
7. 返回權限檢查結果
   ↓
8. TagHelper 根據結果顯示/隱藏元素
```

---

## 🧪 測試步驟

### 1. 基本功能測試

#### 測試案例 1：有完整權限的使用者
**步驟：**
1. 使用具有所有權限的帳號登入
2. 瀏覽 `/ControllerSetting/AccessGroupSearchList`
3. 檢查頁面

**預期結果：**
- ✅ 顯示「新增群組」按鈕
- ✅ 群組名稱可點擊（編輯連結）
- ✅ 顯示「刪除」按鈕

#### 測試案例 2：無權限的使用者
**步驟：**
1. 使用沒有任何權限的帳號登入
2. 瀏覽 `/ControllerSetting/AccessGroupSearchList`
3. 檢查頁面

**預期結果：**
- ❌ 不顯示「新增群組」按鈕
- ❌ 群組名稱不可點擊
- ❌ 不顯示「刪除」按鈕

#### 測試案例 3：部分權限的使用者
**預期結果：**
- ✅ 可以看到列表
- ❌ 不能新增
- ❌ 不能編輯
- ❌ 不能刪除

### 2. 快取測試

#### 測試案例 4：快取機制
**預期結果：**
- 權限不會立即更新（使用快取）
- 等待最多 30 分鐘後權限才會更新

### 3. 效能測試

#### 測試案例 5：查詢次數
**預期結果：**
- 第一次瀏覽時查詢資料庫
- 後續瀏覽使用快取，不查詢資料庫

---

## ⏭️ 待完成工作（建議）

### 1. 其他 Controller 的權限控制
- [ ] Exam Controller 的操作權限
- [ ] Charts Controller 的操作權限
- [ ] SeatMap Controller 的操作權限
- [ ] Tools Controller 的操作權限

### 2. Controller 層權限驗證
建議在 Controller 的 Action 中也加入權限檢查

### 3. 權限管理介面
- [ ] 建立權限管理頁面
- [ ] 提供清除快取功能
- [ ] 顯示使用者權限清單

### 4. 日誌記錄
- [ ] 記錄權限檢查失敗的嘗試
- [ ] 記錄權限變更歷史
- [ ] 建立安全性報表

### 5. 單元測試
- [ ] PermissionService 單元測試
- [ ] PermissionTagHelper 單元測試
- [ ] 整合測試

### 6. 效能優化
- [ ] 考慮使用 Redis 分散式快取（多伺服器環境）
- [ ] 優化資料庫查詢效能
- [ ] 增加更詳細的效能監控

---

## 🔧 維護指南

### 新增權限時
1. 在資料庫的 `ControllerMain` 表中新增記錄
2. 將新功能加入適當的權限群組
3. 在 View 中使用 `permission-check` 標籤
4. （可選）在 Controller 中加入權限驗證
5. 更新本文件的權限清單

### 權限變更後
1. 清除受影響使用者的快取
2. 或等待快取自動過期（最多30分鐘）
3. 或重新啟動應用程式

### 除錯時
1. 檢查 `User.Identity.Name` 是否正確
2. 檢查資料庫中的權限設定
3. 檢查快取是否過期
4. 確認 Controller/Action 名稱正確
5. 查看瀏覽器開發者工具（元素是否被移除）

---

## 📊 效能指標

### 記憶體快取
- **快取類型：** IMemoryCache
- **生命週期：** Singleton
- **過期策略：** 絕對過期 30 分鐘 + 滑動過期 10 分鐘
- **儲存格式：** `HashSet<string>`（Controller.Action 格式）

### 預期效能
- **首次查詢：** ~100-300ms（資料庫查詢 + 樹狀結構處理）
- **快取命中：** <1ms（記憶體存取）
- **記憶體使用：** 每使用者約 1-5KB（取決於權限數量）

---

## 🔐 系統安全性考量

### 已實作
✅ 前端權限檢查（TagHelper）
✅ 記憶體快取（不洩漏敏感資料）
✅ 使用者隔離（每個使用者獨立快取）

### 建議加強
⚠️ 後端權限檢查（Controller層）
⚠️ 操作日誌記錄
⚠️ 異常存取告警

### 注意事項
- 前端檢查不能替代後端驗證
- 敏感操作必須在 Controller 中再次驗證權限
- 快取時間不宜過長（安全性 vs 效能）

---

## 📞 技術支援

如有問題或需要協助，請聯絡開發團隊。

---

**文件版本：** 1.0  
**最後更新：** 2025年10月28日  
**作者：** Cursor AI Assistant

